# Вопросы к коду для точной воронки looksmax (23.07.2026)

> ✅ ОТВЕЧЕНО 23.07 — полные ответы продукта в DEV-ANSWERS-looksmax.md, правки внесены в деталку.

По каждому вопросу указан факт из данных (03–22.07, если не сказано иное), из-за которого он возник.
Самые важные для воронки: **2, 3, 4, 9, 12, 18**.

## Квиз
1. Канонический порядок шагов и условия показа. По данным: age → fix → bugs → holdback → mirrors → rate → gap → growth → gender → math → ratio → ascended → scan. Верно? Ветвления / A/B есть?
2. Почему шаг gender видят ~70% юзеров при 87–90% на соседних шагах? Условный показ (пол уже известен?) или недофаер step-view?
3. funnel-step-view фаерится при каждом показе шага или раз за сессию? При funnel-back-click повторяется?
4. Как попасть на экран загрузки фото без фаера шага scan? У 19% загрузивших фото (2 398 из 12 755) события funnel-step-view/scan нет вообще, другие шаги есть. Deep-link / restore session / возврат после реги?
5. Скип есть только на bugs, mirrors, fix, holdback, rate, age — так и задумано? Почему age скипается (227 юзеров)?

## Фото и генерация
6. photo-upload фаерится на каждую загрузку, включая замену после photo-remove?
7. С какой даты профиль (side) стал опциональным в проде? Сейчас side = 96% от front.
8. generate-click фаерится до или после валидации фото? Можно нажать без обоих фото?

## Рега
9. auth-wall-show показывается всем незалогиненным после generate-click, залогиненные минуют? (Тогда конверсия стены 77% — только по незалогиненным.)
10. На каком URL фаерится login/registration? С pl=/web/looksmax прилетает ~10% рег юзеров looksmax — редирект на /web/auth?
11. После реги юзер возвращается ровно в место флоу? «We could not restore your photos» (126 юзеров/28д) — потеря фото при возврате?

## Скан и тизер
12. scan-complete success — фронтовый показ результата или завершение на бэке? Тизер (9 881) > скан-комплит (9 488): тизер можно открыть без scan-complete?
13. report-view teaser фаерится при каждом открытии? Тизер доступен по прямой ссылке/из истории?
14. «No face detected» (163 юзера/28д) — на каком этапе, куда возвращает? Что делают retry и reset?

## Пейволл и покупка
15. Каждый unlock-tap (cta-bar, after-photo-cell, halos-focus, potential-gauge, sub-score-locked, routine-tease, sub-score-plan-tease) открывает пейволл? Какой?
16. credit-wall-show/subscription vs get feature view — когда какой? В воронке объединены в один шаг «пейволл» — правильно?
17. Покупка всегда на странице виджета (purchase_onetime с pl=/web/looksmax) или есть чекаут с другим URL?

## Полный отчёт и план
18. report-view full — только купившим? 215 юзеров full при 290 купивших: 26% купивших не открыли отчёт, или у части он открывается без события?
19. plan-day-nav next: 1 392 события у 47 юзеров (~30/юзер) — карусель, каждый свайп = событие? План доступен всем купившим?

## Кросс-промо
20. Модалку looksmax-post-onboarding удалили в релизе 03.07 намеренно? Клики cta-* обрываются 02–03.07. Если нет — вернуть: сейчас кросс-промо видят только ~200 купивших вместо всех сгенеривших.
21. Куда ведёт каждая карточка more-widget-click (Hairstyle, Face Shape, Style Analysis, Makeup, Hair Color, Skin Analyzer) — URL? Hairstyle/Hair Color — табы style-analysis или пресеты image-generator?

## Глобальные (из прошлых списков — статус?)
22. test_user не долетает до BQ (0 из 14.8M событий) — фикс тега.
23. sign up view: недосчёт показов ~15–20% после 17.07 — дочинивается?
24. GTM-маппинг dataLayer purchase/registration/begin_checkout/unique_purchase в GA4 (карточные покупки только в плоских, first-payer невидим).
25. twek-лендинг: CTA → generic ai-video-generator вместо ai-twerk-generator (доходит 2%); страницы /web/ai-twek-generator не существует.
